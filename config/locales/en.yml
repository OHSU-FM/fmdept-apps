# Sample localization file for English. Add more files in this directory for other locales.
# See http://github.com/svenfuchs/rails-i18n/tree/master/rails%2Flocale for starting points.

en:
  admin:
      actions:
          show_from_app:
              menu: 'Show In App'
  user:
    notice:
        not_admin: 'Admin status required'
  request:
    reviewer_status:
        0: 'unsubmitted'
        1: 'submitted'
        10: 'in review'
        11: 'missing information'
        120: 'reviewer rejected'
        13: 'awaiting final review'
        20: 'in final review'
        21: 'missing information'
        22: 'rejected by final reviewer'
        23: 'accepted by final reviewer'               
    finalizer_status:
        0: 'unsubmitted'
        1: 'submitted'
        10: 'in review'
        11: 'missing information'
        120: 'reviewer rejected'
        13: 'awaiting final review'
        20: 'in final review'
        21: 'missing information'
        22: 'rejected by final reviewer'
        23: 'accepted by final reviewer'               
    user_status:
        0: 'unsubmitted'
        1: 'submitted'
        10: 'in review'
        11: 'missing information'
        120: 'reviewer rejected'
        13: 'awaiting final review'
        20: 'in final review'
        21: 'missing information'
        22: 'rejected by final reviewer'
        23: 'accepted by final reviewer'               
    desc_status:
        0: is unsubmitted
        1: was submitted
        10: is in review
        11: is missing information
        120: was rejected by the reviewer
        13: is awaiting final review
        20: is in final review
        21: is missing information
        22: was rejected by final reviewer
        23: was accepted by final reviewer             
        
  activerecord:
    models:
        leave_request: leave request
        travel_request: travel request
    attributes:
        user_default_email:
            email: 'E-mail address'
    errors:
      models:
        user_default_email:
          attributes:
            email:
              blank: '%{attribute} is required'
              taken: 'That email address has already been added'
      messages:
        # Default messages: complete sentences
        inclusion: "%{attribute}: is not included in the list"
        exclusion: "%{attribute}: is reserved"
        invalid: "%{attribute}: is invalid"
        confirmation: "%{attribute}: doesn't match confirmation"
        accepted: "%{attribute}: must be accepted"
        empty: "%{attribute}: can't be empty"
        blank: "%{attribute}: can't be blank"
        too_long: "%{attribute}: is too long (maximum is {{count}} characters)"
        too_short: "%{attribute} is too short (minimum is {{count}} characters)"
        wrong_length: "%{attribute}: is the wrong length (should be {{count}} characters)" 
        taken: "%{attribute}: has already been taken"
        not_a_number: "%{attribute}: is not a number"
        greater_than: "%{attribute}: must be greater than {{count}}"
        greater_than_or_equal_to: "%{attribute}: must be greater than or equal to {{count}}"
        equal_to: "%{attribute}: must be equal to {{count}}"
        less_than: "%{attribute}: must be less than {{count}}"
        less_than_or_equal_to: "%{attribute}: must be less than or equal to {{count}}"     
        odd: "%{attribute}: must be odd"
        even: "%{attribute}: must be even"

